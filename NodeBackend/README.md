# Node.js MongoDB Backend for Faculty View

This backend stores the faculty **View** data in MongoDB.

## Run

```bash
cd NodeBackend
npm install
copy .env.example .env
npm start
```

Default URL: `http://localhost:5000`
Default MongoDB: `mongodb://127.0.0.1:27017/student_dashboard_views`

Start MongoDB before running this backend.
